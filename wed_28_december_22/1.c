#include <stdio.h>

int main() {
    float cherpang = 70, music = 0, jameji = 0; //? please initize value to it

    jameji = 70 * 110 / 100;
    music = (jameji + cherpang) / 2;
    
    printf("cherpang = %6.2f\n, music = %6.2f\n, jameji = %6.2f", cherpang, music, jameji);
    return 0;
}